# PostgreSQL
POSTGRES_USER=cottonwood
POSTGRES_PASSWORD=cottonwood
POSTGRES_DB=cottonwood
DATABASE_URL=postgresql://cottonwood:cottonwood@db:5432/cottonwood?schema=public

# API
API_PORT=3000
JWT_SECRET=change_me_super_secret
EMAIL_SETTINGS_ENCRYPTION_KEY=
# EMAIL_SETTINGS_ENCRYPTION_KEY must be base64 for exactly 32 bytes (AES-256-GCM)
# Generate with:
#   node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
# Example format (do not reuse this exact value):
#   EMAIL_SETTINGS_ENCRYPTION_KEY=3xqf8A4f5x4fAqYxQ0g5lYQ6s3fR4nJ2m8w1vT9kL0M=
UPLOAD_DIR=/app/uploads
EXCLUDE_DISPUTED_FROM_VOTING=false
CORS_ORIGIN=http://localhost:5173
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!
# Optional: enable host/subdomain tenant resolution in API (example: sfdemo.manring.co)
TENANT_BASE_DOMAIN=

# Web
VITE_API_BASE_URL=http://localhost:3000/api
# Optional: enable slug-route redirects to subdomain portal URLs in web app
VITE_TENANT_BASE_DOMAIN=
